window.__SNOWBALL_GAME_SRC=(window.__SNOWBALL_GAME_SRC||"")+"active || o.type==='snow' || o===l.source) continue;\n        if(Math.abs(o.z-l.z)<1.5 && Math.abs(o.x-l.x)<1.45 && o.req<=typeData.car.req){\n          o.active=false;\n          const p=o.root.position.clone();\n          const gain=o.gain*.38;\n          state.mass+=gain; state.score+=Math.round((typeData[o.type]?.score||20)*.7);\n          burst(p,o.type, o.type==='car'?16:o.type==='tree'?13:8);\n          o.root.dispose();\n          l.hits++;\n          if(l.hits===1) showToast('\u0426\u0415\u041f\u041d\u0410\u042f \u0420\u0415\u0410\u041a\u0426\u0418\u042f');\n          state.shake=Math.max(state.shake,.22);\n        }\n      }\n      if(l.life<=0 || l.z>state.z+75){ l.mesh.dispose(); rollingLogs.splice(i,1); }\n    }\n  }\n\n  function spawnRollingLogFrom(o){\n    const mesh=BABYLON.MeshBuilder.CreateCylinder('rollingLog',{diameter:.62,height:4.1,tessellation:10},scene);\n    mesh.rotation.z=Math.PI/2; mesh.position.set(o.x,terrainY(o.z)+.34,o.z+.6);\n    const mat=new BABYLON.StandardMaterial('rollingLogMat'+Math.random(),scene); mat.diffuseColor=new BABYLON.Color3(.38,.22,.11); mat.specularColor=BABYLON.Color3.Black(); mesh.material=mat;\n    const snowMat=new BABYLON.StandardMaterial('rollingLogSnow'+Math.random(),scene); snowMat.diffuseColor=new BABYLON.Color3(.95,.98,1); snowMat.specularColor=BABYLON.Color3.Black();\n    for(const x of [-1.4,0,1.4]){ const cap=BABYLON.MeshBuilder.CreateSphere('logSnow',{diameter:.52,segments:6},scene); cap.scaling.set(1,.24,.7); cap.position.set(x,.30,0); cap.parent=mesh; cap.material=snowMat; }\n    rollingLogs.push({mesh,x:o.x,z:o.z+.6,speed:Math.max(18,state.speed+8),life:4.0,hits:0,source:o});\n  }\n\n  function spawnRow(z, cells) {\n    const xs = [-4.8,-2.4,0,2.4,4.8];\n    cells.forEach((t,i) => { if (t) spawnObject(t,xs[i],z); });\n  }\n\n  function spawnObject(type,x,z) {\n    const d = typeData[type];\n    const root = new BABYLON.TransformNode(type+'Root', scene);\n    const worldX = trackCenterX(z)+x;\n    root.position.set(worldX,terrainY(z),z);\n    let body;\n\n    if (type==='snow') {\n      // Flatter snow clump: should read like packed snow on the ground, not a glowing arcade pickup.\n      body = BABYLON.MeshBuilder.CreateSphere('snow', { diameter:1.02, segments:10 }, scene);\n      body.scaling.set(1.10,.34,1.00); body.position.y=.15;\n      const lumpA = BABYLON.MeshBuilder.CreateSphere('snowLumpA',{diameter:.48,segments:8},scene);\n      lumpA.scaling.set(.92,.34,.84); lumpA.position.set(.28,.18,.05); lumpA.parent=root;\n      const lumpB = BABYLON.MeshBuilder.CreateSphere('snowLumpB',{diameter:.40,segments:8},scene);\n      lumpB.scaling.set(.86,.30,.82); lumpB.position.set(-.24,.16,-.04); lumpB.parent=root;\n    } else if (type==='snowman') {\n      const snowMat = new BABYLON.StandardMaterial('snowmanSnow'+Math.random(),scene); snowMat.diffuseColor=new BABYLON.Color3(.96,.99,1); snowMat.specularColor=new BABYLON.Color3(.18,.22,.26); snowMat.specularPower=12;\n      body = BABYLON.MeshBuilder.CreateSphere('snowmanBody',{diameter:1.18,segments:12},scene); body.position.y=.56; body.material=snowMat;\n      const belly = BABYLON.MeshBuilder.CreateSphere('snowmanBelly',{diameter:.90,segments:12},scene); belly.position.y=1.18; belly.parent=root; belly.material=snowMat;\n      const head = BABYLON.MeshBuilder.CreateSphere('snowmanHead',{diameter:.70,segments:12},scene); head.position.y=1.88; head.parent=root; head.material=snowMat;\n      const black = new BABYLON.StandardMaterial('snowmanBlack'+Math.random(),scene); black.diffuseColor=new BABYLON.Color3(.10,.10,.12); black.specularColor=BABYLON.Color3.Black();\n      const scarf = new BABYLON.StandardMaterial('snowmanScarf'+Math.random(),scene); scarf.diffuseColor=new BABYLON.Color3(.78,.10,.10); scarf.specularColor=BABYLON.Color3.Black();\n      const orange = new BABYLON.StandardMaterial('carrot'+Math.random(),scene); orange.diffuseColor=new BABYLON.Color3(1,.45,.08); orange.specularColor=BABYLON.Color3.Black();\n      const nose=BABYLON.MeshBuilder.CreateCylinder('nose',{diameterTop:0,diameterBottom:.13,height:.52,tessellation:8},scene); nose.rotation.x=Math.PI/2; nose.position.set(0,1.86,-.38); nose.parent=root; nose.material=orange;\n      const scarfBand=BABYLON.MeshBuilder.CreateTorus('scarfBand',{diameter:.54,thickness:.10,tessellation:18},scene); scarfBand.rotation.x=Math.PI/2; scarfBand.position.set(0,1.56,-.02); scarfBand.parent=root; scarfBand.material=scarf;\n      const scarfTail=BABYLON.MeshBuilder.CreateBox('scarfTail',{width:.16,height:.52,depth:.28},scene); scarfTail.position.set(.16,1.26,-.28); scarfTail.rotation.z=-.22; scarfTail.parent=root; scarfTail.material=scarf;\n      const hatBrim=BABYLON.MeshBuilder.CreateCylinder('hatBrim',{diameter: .74,height:.06,tessellation:12},scene); hatBrim.position.y=2.16; hatBrim.parent=root; hatBrim.material=black;\n      const hatTop=BABYLON.MeshBuilder.CreateCylinder('hatTop',{diameter:.46,height:.48,tessellation:12},scene); hatTop.position.y=2.38; hatTop.parent=root; hatTop.material=black;\n      for(const ex of [-.12,.12]){ const eye=BABYLON.MeshBuilder.CreateSphere('eye',{diameter:.07,segments:6},scene); eye.position.set(ex,1.93,-.31); eye.parent=root; eye.material=black; }\n      for(const by of [1.48,1.24,.99]){ const btn=BABYLON.MeshBuilder.CreateSphere('btn',{diameter:.08,segments:6},scene); btn.position.set(0,by,-.42); btn.parent=root; btn.material=black; }\n      for(const side of [-1,1]){ const arm=BABYLON.MeshBuilder.CreateCylinder('arm',{diameter:.05,height:.72,tessellation:6},scene); arm.position.set(side*.58,1.36,-.02); arm.rotation.z=side*1.08; arm.rotation.x=.26; arm.parent=root; arm.material=black; }\n      const baseSnow=BABYLON.MeshBuilder.CreateSphere('snowmanBaseSnow',{diameter:1.55,segments:6},scene); baseSnow.scaling.set(1,.10,1); baseSnow.position.y=.08; baseSnow.parent=root; baseSnow.material=snowMat;\n    } else if (type==='fence') {\n      const wood = new BABYLON.StandardMaterial('fenceWood'+Math.random(),scene); wood.diffuseColor=new BABYLON.Color3(.55,.32,.16); wood.specularColor=BABYLON.Color3.Black();\n      const snowCap = new BABYLON.StandardMaterial('fenceSnow'+Math.random(),scene); snowCap.diffuseColor=new BABYLON.Color3(.95,.98,1); snowCap.specularColor=BABYLON.Color3.Black();\n      body = BABYLON.MeshBuilder.CreateBox('fenceRail',{width:2.18,height:.16,depth:.12},scene); body.position.y=.46; body.material=wood;\n      const topRail=BABYLON.MeshBuilder.CreateBox('fenceTopRail',{width:2.18,height:.16,depth:.12},scene); topRail.position.y=.82; topRail.parent=root; topRail.material=wood;\n      for (const px of [-.88,0,.88]) { const post=BABYLON.MeshBuilder.CreateBox('post',{width:.18,height:1.02,depth:.18},scene); post.position.set(px,.51,0); post.parent=root; post.material=wood; const cap=BABYLON.MeshBuilder.CreateSphere('postCap',{diameter:.22,segments:6},scene); cap.scaling.y=.35; cap.position.set(px,1.03,0); cap.parent=root; cap.material=snowCap; }\n      const snow=BABYLON.MeshBuilder.CreateSphere('fenceSnowTop',{diameter:2.20,segments:6},scene); snow.scaling.set(1,.10,.22); snow.position.y=.95; snow.parent=root; snow.material=snowCap;\n    } else if (type==='tree') {\n      body = BABYLON.MeshBuilder.CreateCylinder('trunk',{diameter:.30,height:1.45,tessellation:8},scene); body.position.y=.72;\n      const trunkMat=new BABYLON.StandardMaterial('treeTrunk'+Math.random(),scene); trunkMat.diffuseColor=new BABYLON.Color3(.28,.18,.10); trunkMat.specularColor=BABYLON.Color3.Black(); body.material=trunkMat;\n      const leafMat=new BABYLON.StandardMaterial('treeLeaf'+Math.random(),scene); leafMat.diffuseColor=new BABYLON.Color3(.16,.38,.22); leafMat.specularColor=BABYLON.Color3.Black();\n      const snowMat=new BABYLON.StandardMaterial('treeSnow'+Math.random(),scene); snowMat.diffuseColor=new BABYLON.Color3(.95,.98,1); snowMat.specularColor=BABYLON.Color3.Black();\n      const crown1=BABYLON.MeshBuilder.CreateCylinder('crown1',{diameterTop:0,diameterBottom:2.15,height:2.4,tessellation:8},scene); crown1.position.y=1.78; crown1.parent=root; crown1.material=leafMat;\n      const crown2=BABYLON.MeshBuilder.CreateCylinder('crown2',{diameterTop:0,diameterBottom:1.62,height:2.0,tessellation:8},scene); crown2.position.y=2.92; crown2.parent=root; crown2.material=leafMat;\n      const snow1=BABYLON.MeshBuilder.CreateSphere('treeSnow1',{diameter:1.36,segments:6},scene); snow1.scaling.set(1,.20,1); snow1.position.set(0,1.52,.05); snow1.parent=root; snow1.material=snowMat;\n      const snow2=BABYLON.MeshBuilder.CreateSphere('treeSnow2',{diameter:1.02,segments:6},scene); snow2.scaling.set(1,.18,1); snow2.position.set(0,2.58,.03); snow2.parent=root; snow2.material=snowMat;\n      const baseSnow=BABYLON.MeshBuilder.CreateSphere('treeBaseSnow',{diameter:1.5,segments:6},scene); baseSnow.scaling.set(1,.10,1); baseSnow.position.y=.08; baseSnow.parent=root; baseSnow.material=snowMat;\n    } else if (type==='car') {\n      body = BABYLON.MeshBuilder.CreateBox('car',{width:2.2,height:.62,depth:3.08},scene); body.position.y=.60;\n      const bodyMat=new BABYLON.StandardMaterial('carBody'+Math.random(),scene); bodyMat.diffuseColor=d.color; bodyMat.specularColor=new BABYLON.Color3(.18,.18,.18); bodyMat.specularPower=18;\n      body.material=bodyMat;\n      const cabin=BABYLON.MeshBuilder.CreateBox('cabin',{width:1.68,height:.56,depth:1.45},scene); cabin.position.set(0,1.05,.12); cabin.parent=root;\n      const glass=new BABYLON.StandardMaterial('carGlass'+Math.random(),scene); glass.diffuseColor=new BABYLON.Color3(.18,.28,.34); glass.emissiveColor=new BABYLON.Color3(.03,.05,.06); glass.specularColor=BABYLON.Color3.Black(); cabin.material=glass;\n      const carSnowMat=new BABYLON.StandardMaterial('carSnow'+Math.random(),scene); carSnowMat.diffuseColor=new BABYLON.Color3(.95,.98,1); carSnowMat.specularColor=BABYLON.Color3.Black();\n      const roofSnow=BABYLON.MeshBuilder.CreateBox('carRoofSnow',{width:1.55,height:.12,depth:1.26},scene); roofSnow.position.set(0,1.36,.12); roofSnow.parent=root; roofSnow.material=carSnowMat;\n      const bumperMat=new BABYLON.StandardMaterial('carBumper'+Math.random(),scene); bumperMat.diffuseColor=new BABYLON.Color3(.18,.20,.22); bumperMat.specularColor=BABYLON.Color3.Black();\n      const underBody=BABYLON.MeshBuilder.CreateBox('underBody',{width:2.08,height:.14,depth:2.70},scene); underBody.position.set(0,.34,.06); underBody.parent=root; underBody.material=bumperMat;\n      for (const zf of [-1.48,1.48]) { const bumper=BABYLON.MeshBuilder.CreateBox('bumper',{width:2.06,height:.18,depth:.14},scene); bumper.position.set(0,.44,zf); bumper.parent=root; bumper.material=bumperMat; }\n      const lightMat=new BABYLON.StandardMaterial('carLight'+Math.random(),scene); lightMat.emissiveColor=new BABYLON.Color3(.96,.90,.68); lightMat.diffuseColor=new BABYLON.Color3(.72,.72,.58); lightMat.specularColor=BABYLON.Color3.Black();\n      const rearMat=new BABYLON.StandardMaterial('rearLight'+Math.random(),scene); rearMat.emissiveColor=new BABYLON.Color3(.60,.08,.06); rearMat.diffuseColor=new BABYLON.Color3(.42,.08,.07); rearMat.specularColor=BABYLON.Color3.Black();\n      for (const side of [-1,1]) {\n        const head=BABYLON.MeshBuilder.CreateBox('headLight',{width:.24,height:.12,depth:.08},scene); head.position.set(side*.72,.66,-1.56); head.parent=root; head.material=lightMat;\n        const tail=BABYLON.MeshBuilder.CreateBox('tailLight',{width:.24,height:.12,depth:.08},scene); tail.position.set(side*.72,.66,1.56); tail.parent=root; tail.material=rearMat;\n        const sideWin=BABYLON.MeshBuilder.CreateBox('sideWin',{width:.08,height:.34,depth:.78},scene); sideWin.position.set(side*.85,1.05,.12); sideWin.parent=root; sideWin.material=glass;\n      }\n      for (const wx of [-.86,.86]) for (const wz of [-.94,.94]) { const w=BABYLON.MeshBuilder.CreateCylinder('wheel',{diameter:.50,height:.20,tessellation:12},scene); w.rotation.z=Math.PI/2; w.position.set(wx,.34,wz); w.parent=root; w.material=bumperMat; const hub=BABYLON.MeshBuilder.CreateCylinder('hub',{diameter:.20,height:.22,tessellation:10},scene); hub.rotation.z=Math.PI/2; hu";
