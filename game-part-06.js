window.__SNOWBALL_GAME_SRC=(window.__SNOWBALL_GAME_SRC||"")+"eRim',{width:.22,height:.11,depth:2.72},scene);\n      rim.position.set(edge,.10,0); rim.rotation.z=(Math.random()-.5)*.10; rim.parent=root; rim.material=iceMat;\n    }\n    for(let x=a+.55;x<b-.25;x+=1.05){\n      const shard=BABYLON.MeshBuilder.CreatePolyhedron('crackShard',{type:2,size:.28+Math.random()*.18},scene);\n      shard.scaling.y=.62; shard.position.set(x,.18,(Math.random()-.5)*2.05); shard.rotation.set(Math.random(),Math.random(),Math.random()); shard.parent=root; shard.material=iceMat;\n    }\n    for(const zoff of [-1.6,1.6]){\n      const board=BABYLON.MeshBuilder.CreateBox('crevasseBoard',{width:Math.max(1.4,w*.85),height:.24,depth:.10},scene);\n      board.position.set((a+b)*.5,.62,zoff); board.parent=root; board.material=poleRed;\n      const board2=BABYLON.MeshBuilder.CreateBox('crevasseBoardStripe',{width:Math.max(1.4,w*.85),height:.10,depth:.11},scene);\n      board2.position.set((a+b)*.5,.62,zoff); board2.parent=root; board2.material=poleWhite;\n    }\n  }\n  for(const px of [gapL, gapR]){\n    for(const pz of [-1.45,1.45]){\n      const pole=BABYLON.MeshBuilder.CreateCylinder('crevassePole',{diameter:.11,height:1.45,tessellation:8},scene);\n      pole.position.set(px,.72,pz); pole.parent=root; pole.material=poleWhite;\n      const band=BABYLON.MeshBuilder.CreateCylinder('crevassePoleBand',{diameter:.12,height:.36,tessellation:8},scene);\n      band.position.set(px,.88,pz); band.parent=root; band.material=poleRed;\n    }\n  }\n  crevasseTraps.push({root,z,center:cx,gapX,gapWidth,active:true});\n}\n\nfunction createIceSpikeTrap(z,side='left',safeWidth=8.0){\n  const root=new BABYLON.TransformNode('iceSpikeTrap',scene);\n  const cx=trackCenterX(z);\n  root.position.set(cx,terrainY(z),z);\n  root.rotation.y=Math.atan(trackTurn(z));\n\n  const iceMat=new BABYLON.StandardMaterial('spikeIce'+z,scene);\n  iceMat.diffuseColor=new BABYLON.Color3(.66,.98,.99);\n  iceMat.emissiveColor=new BABYLON.Color3(.18,.36,.42);\n  iceMat.specularColor=new BABYLON.Color3(.22,.32,.36);\n  iceMat.specularPower=34;\n\n  const baseMat=new BABYLON.StandardMaterial('spikeBase'+z,scene);\n  baseMat.diffuseColor=new BABYLON.Color3(.05,.12,.18);\n  baseMat.emissiveColor=new BABYLON.Color3(.02,.05,.08);\n  baseMat.specularColor=BABYLON.Color3.Black();\n  baseMat.zOffset=-1;\n\n  const glowMat=new BABYLON.StandardMaterial('spikeGlow'+z,scene);\n  glowMat.diffuseColor=new BABYLON.Color3(.56,.96,.98);\n  glowMat.emissiveColor=new BABYLON.Color3(.20,.40,.44);\n  glowMat.alpha=.68;\n  glowMat.specularColor=BABYLON.Color3.Black();\n  glowMat.zOffset=-2;\n\n  const poleRed=new BABYLON.StandardMaterial('spikePoleRed'+z,scene);\n  poleRed.diffuseColor=new BABYLON.Color3(.90,.16,.16);\n  poleRed.specularColor=BABYLON.Color3.Black();\n  const poleWhite=new BABYLON.StandardMaterial('spikePoleWhite'+z,scene);\n  poleWhite.diffuseColor=new BABYLON.Color3(.98,.99,1);\n  poleWhite.specularColor=BABYLON.Color3.Black();\n\n  // The trap is now a one-sided spike field. A guaranteed corridor remains open\n  // for the largest expected snowball at this point in the level.\n  const half=CFG.trackHalfWidth;\n  safeWidth=Math.min(safeWidth, half*2-2.0);\n  const boundary = side==='left' ? half-safeWidth : -half+safeWidth;\n  const hazardA = side==='left' ? -half : boundary;\n  const hazardB = side==='left' ? boundary : half;\n  const hazardCenter=(hazardA+hazardB)*.5;\n  const hazardWidth=Math.max(1.8,hazardB-hazardA);\n\n  const field=BABYLON.MeshBuilder.CreateBox('spikeField',{width:hazardWidth,height:.055,depth:4.8},scene);\n  field.position.set(hazardCenter,.10,0);\n  field.parent=root; field.material=baseMat;\n\n  const fieldGlow=BABYLON.MeshBuilder.CreateBox('spikeFieldGlow',{width:hazardWidth-.16,height:.025,depth:4.55},scene);\n  fieldGlow.position.set(hazardCenter,.135,0);\n  fieldGlow.parent=root; fieldGlow.material=glowMat;\n\n  const clusters=[];\n  const spacing=1.35;\n  for(let lx=hazardA+.75; lx<=hazardB-.45; lx+=spacing){\n    for(let row=0; row<2; row++){\n      const zoff=row===0?-.85:.85;\n      const h=1.65+Math.random()*1.55;\n      const spike=BABYLON.MeshBuilder.CreateCylinder('iceSpike',{diameterTop:0,diameterBottom:.42+Math.random()*.22,height:h,tessellation:6},scene);\n      spike.position.set(lx+(Math.random()-.5)*.35,h*.5+.10,zoff+(Math.random()-.5)*.35);\n      spike.rotation.z=(Math.random()-.5)*.14;\n      spike.rotation.x=(Math.random()-.5)*.10;\n      spike.parent=root; spike.material=iceMat;\n      spike.renderOutline=true;\n      spike.outlineColor=new BABYLON.Color3(.04,.14,.20);\n      spike.outlineWidth=.035;\n    }\n  }\n\n  // Collision is represented by one continuous hazard interval rather than\n  // several tiny circles. This matches what the player sees.\n  clusters.push({x:cx+hazardCenter,radius:hazardWidth*.5});\n\n  // Red/white boundary posts clearly mark the edge of the safe corridor.\n  for(const pz of [-2.55,2.55]){\n    const pole=BABYLON.MeshBuilder.CreateCylinder('spikeBoundaryPole',{diameter:.12,height:1.55,tessellation:8},scene);\n    pole.position.set(boundary,.78,pz); pole.parent=root; pole.material=poleWhite;\n    const band=BABYLON.MeshBuilder.CreateCylinder('spikeBoundaryBand',{diameter:.13,height:.42,tessellation:8},scene);\n    band.position.set(boundary,.96,pz); band.parent=root; band.material=poleRed;\n  }\n\n  iceSpikeTraps.push({root,z,clusters,active:true,side,safeWidth,boundary});\n}\n\nfunction createRockfallTrap(z){\n  const root=new BABYLON.TransformNode('rockfallTrap',scene);\n  const cx=trackCenterX(z);\n  root.position.set(cx,terrainY(z)+14,z);\n  root.setEnabled(false);\n  const rockMat=new BABYLON.StandardMaterial('fallRock'+z,scene);\n  rockMat.diffuseColor=new BABYLON.Color3(.14,.16,.18); rockMat.specularColor=BABYLON.Color3.Black();\n  const snowMat=new BABYLON.StandardMaterial('fallRockSnow'+z,scene);\n  snowMat.diffuseColor=new BABYLON.Color3(.94,.98,1); snowMat.specularColor=BABYLON.Color3.Black();\n  const rock=BABYLON.MeshBuilder.CreatePolyhedron('fallingBoulder',{type:2,size:1.55},scene);\n  rock.scaling.set(1.28,1.12,1.15); rock.parent=root; rock.material=rockMat;\n  const cap=BABYLON.MeshBuilder.CreateSphere('fallRockCap',{diameter:1.55,segments:6},scene);\n  cap.scaling.set(.58,.10,.40); cap.position.set(-.26,.88,.02); cap.parent=root; cap.material=snowMat;\n  const shadow=BABYLON.MeshBuilder.CreateDisc('rockfallShadow',{radius:1.45,tessellation:24},scene);\n  shadow.rotation.x=Math.PI/2+SLOPE_ANGLE;\n  shadow.position.set(cx,terrainY(z)+.04,z);\n  const shMat=new BABYLON.StandardMaterial('rockfallShadowMat'+z,scene);\n  shMat.diffuseColor=new BABYLON.Color3(.04,.10,.12); shMat.alpha=0; shadow.material=shMat;\n  rockfallTraps.push({root,shadow,z,center:cx,targetX:cx,triggered:false,timer:0,done:false,radius:1.45});\n}\n\n  function spawnLethalRock(z,amp=7,speed=1.05,phase=0){\n    const root=new BABYLON.TransformNode('lethalRock',scene);\n    const cx=trackCenterX(z);\n    root.position.set(cx,terrainY(z),z);\n    const rockMat=new BABYLON.StandardMaterial('lethalRockMat'+z,scene);\n    rockMat.diffuseColor=new BABYLON.Color3(.17,.19,.21);\n    rockMat.ambientColor=new BABYLON.Color3(.10,.12,.14);\n    rockMat.specularColor=BABYLON.Color3.Black();\n    const snowMat=new BABYLON.StandardMaterial('lethalRockSnow'+z,scene);\n    snowMat.diffuseColor=new BABYLON.Color3(.94,.98,1); snowMat.specularColor=BABYLON.Color3.Black();\n    const rock=BABYLON.MeshBuilder.CreatePolyhedron('deathBoulder',{type:2,size:1.55},scene);\n    rock.scaling.set(1.35,1.12,1.18); rock.position.y=1.15; rock.rotation.set(.3,.6,.2); rock.parent=root; rock.material=rockMat;\n    const chunk=BABYLON.MeshBuilder.CreatePolyhedron('deathBoulderChunk',{type:1,size:.72},scene);\n    chunk.scaling.set(1.2,.8,1); chunk.position.set(.68,1.35,.12); chunk.rotation.set(.4,.2,.5); chunk.parent=root; chunk.material=rockMat;\n    const snow=BABYLON.MeshBuilder.CreateSphere('deathBoulderSnow',{diameter:1.75,segments:7},scene);\n    snow.scaling.set(.58,.10,.40); snow.position.set(-.28,1.90,.06); snow.parent=root; snow.material=snowMat;\n    const shadow=BABYLON.MeshBuilder.CreateDisc('deathBoulderShadow',{radius:1.3,tessellation:24},scene);\n    shadow.rotation.x=Math.PI/2; shadow.position.y=.035; shadow.parent=root;\n    const sm=new BABYLON.StandardMaterial('deathBoulderShadowMat'+z,scene); sm.diffuseColor=new BABYLON.Color3(.04,.10,.12); sm.alpha=.48; shadow.material=sm;\n    for(const side of [-1,1]){ const pole=BABYLON.MeshBuilder.CreateCylinder('lethalPole',{diameter:.11,height:1.28,tessellation:8},scene); pole.position.set(side*(CFG.trackHalfWidth-.4),.64,0); pole.parent=root; const pm=new BABYLON.StandardMaterial('lethalPoleMat'+z+side,scene); pm.diffuseColor=new BABYLON.Color3(.92,.16,.16); pm.specularColor=BABYLON.Color3.Black(); pole.material=pm; }\n    lethalRocks.push({root,z,center:cx,x:cx,amp,speed,phase,radius:1.55,spin:0});\n  }\n\n\nfunction updateDynamicObjects(dt){\n  // Static lethal cracks: choose the visible safe opening.\n  for(const h of crevasseTraps){\n    if(!h.active || state.runEnded || state.finished) continue;\n    if(Math.abs(state.z-h.z)>1.55) continue;\n    if(state.airborne && state.airY>.72) continue;\n    const localX=state.x-h.center;\n    const safe=Math.abs(localX-h.gapX) < h.gapWidth*.5;\n    if(!safe){ crashIntoTrap('\u041b\u0415\u0414\u042f\u041d\u0410\u042f \u0422\u0420\u0415\u0429\u0418\u041d\u0410','\u0412 \u0442\u0440\u0435\u0449\u0438\u043d\u0443 \u043d\u0435\u043b\u044c\u0437\u044f \u043f\u0440\u043e\u0432\u0430\u043b\u0438\u0432\u0430\u0442\u044c\u0441\u044f \u2014 \u0438\u0449\u0438 \u0448\u0438\u0440\u043e\u043a\u0438\u0439 \u0431\u0435\u0437\u043e\u043f\u0430\u0441\u043d\u044b\u0439 \u0440\u0430\u0437\u0440\u044b\u0432 \u0437\u0430\u0440\u0430\u043d\u0435\u0435.'); return; }\n  }\n\n  // Fixed ice spikes: pure slalom hazard, never edible.\n  for(const h of iceSpikeTraps){\n    if(!h.active || state.runEnded || state.finished) continue;\n    if(Math.abs(state.z-h.z)>2.0) continue;\n    if(state.airborne && state.airY>.85) continue;\n    for(const c of h.clusters){\n      const hitR=c.radius;\n      if(Math.abs(state.x-c.x)<hitR){ crashIntoTrap('\u041b\u0415\u0414\u042f\u041d\u042b\u0415 \u041f\u0418\u041a\u0418','\u041b\u0435\u0434\u044f\u043d\u044b\u0435 \u043f\u0438\u043a\u0438 \u0441\u043c\u0435\u0440\u0442\u0435\u043b\u044c\u043d\u044b \u043f\u0440\u0438 \u043b\u044e\u0431\u043e\u0439 \u043c\u0430\u0441\u0441\u0435 \u2014 \u0434\u0435\u0440\u0436\u0438\u0441\u044c \u0432 \u0448\u0438\u0440\u043e\u043a\u043e\u043c \u0441\u0432\u043e\u0431\u043e\u0434\u043d\u043e\u043c \u043a\u043e\u0440\u0438\u0434\u043e\u0440\u0435.'); return; }\n    }\n  }\n\n  // Rockfall: target the lane the player occupies, then give ~1 second to escape the shadow.\n  for(const h of rockfallTraps){\n    if(h.done) continue;\n    if(!h.triggered && state.z>=h.z-24){\n      h.triggered=true; h.timer=0;\n      const minX=h.center-CFG.trackHalfWidth+1.5, maxX=h.center+CFG.trackHalfWidth-1.5;\n      h.targetX=BABYLON.Scalar.Clamp(state.x,minX,maxX);\n      h.root.position.x=h.targetX; h.shadow.position.x=h.targetX;\n      h.root.setEnabled(true);\n      if(h.shadow.material) h.shadow.material.alpha=.16;\n    }\n    if(!h.triggered) continue;\n    h.timer+=dt;\n    const t=BABYLON.Scalar.Clamp(h.timer/.95,0,1);\n    const fallEase=t*t;\n    h.root.position.y=terrainY(h.z)+13.5-(12.15*fallEase);\n    h.root.rotation.x+=dt*4.8; h.root.rotation.z+=dt*3.5;\n    if(h.shadow.material) h.shadow.material.alpha=.24+.62*t;\n    h.shadow.scaling.setAll(.55+.70*t);\n    if(!state.runEnded && !state.finished && Math.abs(state.z-h.z)<2.2 && t>.58 && t<1.18){\n      if(!(state.airborne && state.airY>1.05) && Math.abs(state.x-h.targetX)<h.radius+Math.min(.65,ball.scaling.x*.16)){\n        crashIntoTrap('\u041a\u0410\u041c\u041d\u0415\u041f\u0410\u0414','\u0421\u043c\u043e\u0442\u0440\u0438 \u043d\u0430 \u0440\u0430\u0441\u0442\u0443\u0449\u0443\u044e \u0442\u0435\u043d\u044c: \u043f\u0430\u0434\u0430\u044e\u0449\u0430\u044f \u0433\u043b\u044b\u0431\u0430 \u0431\u044c\u0451\u0442 \u0442\u0443\u0434\u0430, \u0433\u0434\u0435 \u0442\u044b \u0435\u0445\u0430\u043b \u0441\u0435\u043a\u0443\u043d\u0434\u0443 \u043d\u0430\u0437\u0430\u0434.'); return;\n      }\n    }\n    if(h.timer>1.35){\n      h.done=true;\n      if(h.root && !h.root.isDisposed()) h.root.dispose();\n      if(h.shadow && !h.shadow.isDisposed()) h.shadow.dispose();\n    }\n  }\n    for(const h of lethalRocks){\n      const local=Math.sin(state.elapsed*h.speed+h.phase)*h.amp;\n      h.x=h.center+local;\n      h.root.position.x=h.x;\n      h.spin += dt*(1.9+h.speed*.8);\n      h.root.rotation.z=h.spin;\n      h.root.rotation.x=h.spin*.72;\n      if(state.runEnded || state.finished) continue;\n      if(state.airborne && state.airY>.95) continue;\n      const dz=Math.abs(state.z-h.z);\n      if(dz>2.5) continue;\n      const hitR=ball.scaling.x*.62+h.radius;\n      if(Math.abs(state.x-h.x)<=hitR && dz<=1.9){\n        crashIntoRock(h);\n        break;\n      }\n    }\n    for(let i=rollingLogs.length-1;i>=0;i--){\n      const l=rollingLogs[i]; l.life-=dt; l.z+=l.speed*dt; l.mesh.position.z=l.z; l.mesh.position.y=terrainY(l.z)+.34; l.mesh.rotation.x+=l.speed*dt*1.2;\n      for(const o of obstacles){\n        if(!o.";
