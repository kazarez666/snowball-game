window.__SNOWBALL_GAME_SRC=(window.__SNOWBALL_GAME_SRC||"")+"(() => {\n  'use strict';\n\n  const $ = (id) => document.getElementById(id);\n  const ui = {\n    mass: $('mass'), best: $('best'), progress: $('progress'), combo: $('combo'), toast: $('toast'),\n    tutorial: $('tutorial'), result: $('result'), resultTitle: $('resultTitle'), resultText: $('resultText'),\n    finalMass: $('finalMass'), destroyed: $('destroyed'), score: $('score'), starCount: $('starCount'),\n    upStart: $('upStart'), upSnow: $('upSnow'), upImpact: $('upImpact'), errorBox: $('errorBox'),\n    goalText: $('goalText'), goalFill: $('goalFill'), goalLabel: $('goalLabel'), goalNext: $('goalNext'), powerGoal: $('powerGoal'),\n    resultBadge: $('resultBadge'), recordLine: $('recordLine'), earnedLine: $('earnedLine'),\n    costStart: $('costStart'), costSnow: $('costSnow'), costImpact: $('costImpact'), upgradeHint: $('upgradeHint'),\n    campaignView: $('campaignView'), levelBrief: $('levelBrief'), level1Btn: $('level1Btn'), level2Btn: $('level2Btn'),\n    level1Status: $('level1Status'), level2Status: $('level2Status'), chapterCount: $('chapterCount'),\n    briefLevel: $('briefLevel'), briefTitle: $('briefTitle'), briefText: $('briefText'), levelHudNo: $('levelHudNo'),\n    campaignBtn: $('campaignBtn'), briefBackBtn: $('briefBackBtn'),\n    goldCount: $('goldCount'), goldHudCount: $('goldHudCount'), goldReward: $('goldReward'), baseGold: $('baseGold'),\n    finishMultiplier: $('finishMultiplier'), goldEarned: $('goldEarned'), goldRewardNote: $('goldRewardNote')\n  };\n\n  const params = new URLSearchParams(window.location.search);\n  const requestedLevel = Number(params.get('level'));\n  let activeLevel = requestedLevel === 2 ? 2 : 1;\n  const openedFromCampaign = requestedLevel === 1 || requestedLevel === 2;\n  const levelInfo = {\n    1:{title:'\u0413\u043e\u0440\u043d\u0430\u044f \u0434\u0435\u0440\u0435\u0432\u043d\u044f', subtitle:'\u041b\u0435\u0441, \u0434\u0435\u0440\u0435\u0432\u043d\u044f \u0438 \u043f\u0435\u0440\u0432\u044b\u0435 \u0441\u0435\u0440\u044c\u0451\u0437\u043d\u044b\u0435 \u0440\u0443\u0431\u0435\u0436\u0438.', finalMass:900, baseGold:100},\n    2:{title:'\u041b\u0435\u0434\u044f\u043d\u043e\u0439 \u043a\u0443\u0440\u043e\u0440\u0442', subtitle:'\u0411\u043e\u043b\u044c\u0448\u0435 \u043b\u044c\u0434\u0430, \u0434\u0440\u0443\u0433\u0438\u0435 \u043f\u043e\u0432\u043e\u0440\u043e\u0442\u044b \u0438 \u043e\u043f\u0430\u0441\u043d\u0435\u0435 \u043b\u0438\u043d\u0438\u0438 \u0434\u0432\u0438\u0436\u0435\u043d\u0438\u044f.', finalMass:980, baseGold:120}\n  };\n  const finishMultipliers = [1, 1.25, 1.5, 1.75, 2, 3];\n  const skinDefs = {\n    classic:{name:'\u041a\u043b\u0430\u0441\u0441\u0438\u0447\u0435\u0441\u043a\u0438\u0439',cost:0,diffuse:[.97,.99,1],ambient:[.74,.86,.96],emissive:[.05,.10,.14],specular:[.30,.38,.46],outline:[.23,.43,.60]},\n    ice:{name:'\u041b\u0435\u0434\u044f\u043d\u043e\u0439',cost:180,diffuse:[.68,.90,1],ambient:[.44,.72,.88],emissive:[.06,.20,.29],specular:[.68,.88,1],outline:[.22,.78,1]},\n    mint:{name:'\u041c\u044f\u0442\u043d\u044b\u0439',cost:300,diffuse:[.72,1,.90],ambient:[.42,.78,.67],emissive:[.04,.19,.13],specular:[.55,.88,.76],outline:[.20,.72,.56]},\n    berry:{name:'\u042f\u0433\u043e\u0434\u043d\u044b\u0439',cost:450,diffuse:[.94,.50,.66],ambient:[.72,.30,.45],emissive:[.20,.04,.09],specular:[.72,.42,.54],outline:[.82,.24,.42]},\n    gold:{name:'\u0417\u043e\u043b\u043e\u0442\u043e\u0439',cost:650,diffuse:[1,.78,.22],ambient:[.78,.52,.08],emissive:[.23,.13,.01],specular:[1,.91,.55],outline:[.92,.58,.05]}\n  };\n\n  if (!window.BABYLON) {\n    ui.errorBox.classList.remove('hidden');\n    ui.errorBox.textContent = '\u041d\u0435 \u0443\u0434\u0430\u043b\u043e\u0441\u044c \u0437\u0430\u0433\u0440\u0443\u0437\u0438\u0442\u044c Babylon.js.\\n\\n\u041f\u0440\u043e\u0432\u0435\u0440\u044c \u0438\u043d\u0442\u0435\u0440\u043d\u0435\u0442 \u0438 \u043f\u0435\u0440\u0435\u0437\u0430\u0433\u0440\u0443\u0437\u0438 \u0441\u0442\u0440\u0430\u043d\u0438\u0446\u0443.';\n    return;\n  }\n\n  const CFG = {\n    trackHalfWidth: 8.35,\n    levelLength: 1450,\n    finishZ: 1210,\n    baseMass: 8,\n    baseSpeed: 10.6,\n    maxSpeed: 28.5,\n    steerSpeed: 12.0,\n    hitCooldown: 0.22,\n    cameraHeight: 4.6,\n    cameraBack: 8.2\n  };\n\n  const SLOPE_DROP_PER_Z = .076;\n  const SLOPE_START_Y = 22.0;\n  const SLOPE_ANGLE = Math.atan(SLOPE_DROP_PER_Z);\n  const terrainY = (z) => SLOPE_START_Y - z * SLOPE_DROP_PER_Z;\n\n  // v1.0: a broad S-shaped downhill line. The player still only steers left/right,\n  // but the mountain itself bends underneath them instead of feeling like a flat conveyor.\n  const smooth01 = (t) => { t=BABYLON.Scalar.Clamp(t,0,1); return t*t*(3-2*t); };\n  \n\nfunction trackCenterLevel1(z) {\n    if (z < 42) return 0;\n    if (z < 86) return 3.8*smooth01((z-42)/44);\n    if (z < 132) return 3.8 + (-7.4)*smooth01((z-86)/46);\n    if (z < 176) return -3.6 + 6.0*smooth01((z-132)/44);\n    if (z < 218) return 2.4 + (-5.2)*smooth01((z-176)/42);\n    if (z < 268) return -2.8 + 5.6*smooth01((z-218)/50);\n    if (z < 330) return 2.8 + (-6.5)*smooth01((z-268)/62);\n    if (z < 395) return -3.7 + 7.3*smooth01((z-330)/65);\n    if (z < 465) return 3.6 + (-6.8)*smooth01((z-395)/70);\n    if (z < 525) return -3.2 + 7.0*smooth01((z-465)/60);\n    if (z < 595) return 3.8 + (-7.1)*smooth01((z-525)/70);\n    if (z < 665) return -3.3 + 7.4*smooth01((z-595)/70);\n    if (z < 735) return 4.1 + (-8.0)*smooth01((z-665)/70);\n    if (z < 805) return -3.9 + 7.5*smooth01((z-735)/70);\n    if (z < 865) return 3.6 + (-6.4)*smooth01((z-805)/60);\n    if (z < 930) return -2.8 + 6.2*smooth01((z-865)/65);\n    if (z < 1005) return 3.4 + (-7.0)*smooth01((z-930)/75);\n    if (z < 1080) return -3.6 + 7.8*smooth01((z-1005)/75);\n    if (z < 1155) return 4.2 + (-7.2)*smooth01((z-1080)/75);\n    if (z < 1210) return -3.0 + 3.0*smooth01((z-1155)/55);\n    return 0;\n  }\n\n  // Level 2 uses a distinct rhythm: longer opening sweeps, a tight middle S and a straighter resort finish.\n  function trackCenterLevel2(z) {\n    if (z < 55) return 0;\n    if (z < 120) return -4.2*smooth01((z-55)/65);\n    if (z < 190) return -4.2 + 8.5*smooth01((z-120)/70);\n    if (z < 265) return 4.3 + (-7.0)*smooth01((z-190)/75);\n    if (z < 345) return -2.7 + 6.6*smooth01((z-265)/80);\n    if (z < 420) return 3.9 + (-8.3)*smooth01((z-345)/75);\n    if (z < 505) return -4.4 + 7.4*smooth01((z-420)/85);\n    if (z < 585) return 3.0 + (-5.6)*smooth01((z-505)/80);\n    if (z < 650) return -2.6 + 7.0*smooth01((z-585)/65);\n    if (z < 715) return 4.4 + (-8.8)*smooth01((z-650)/65);\n    if (z < 785) return -4.4 + 8.1*smooth01((z-715)/70);\n    if (z < 865) return 3.7 + (-6.0)*smooth01((z-785)/80);\n    if (z < 945) return -2.3 + 5.9*smooth01((z-865)/80);\n    if (z < 1020) return 3.6 + (-7.6)*smooth01((z-945)/75);\n    if (z < 1100) return -4.0 + 6.4*smooth01((z-1020)/80);\n    if (z < 1170) return 2.4 + (-4.2)*smooth01((z-1100)/70);\n    if (z < 1210) return -1.8 + 1.8*smooth01((z-1170)/40);\n    return 0;\n  }\n\n  function trackCenterX(z) {\n    return activeLevel === 2 ? trackCenterLevel2(z) : trackCenterLevel1(z);\n  }\n\n  function trackTurn(z) {\n    const d=.8;\n    return (trackCenterX(z+d)-trackCenterX(z-d))/(d*2);\n  }\n\n  const thresholds = [0, 16, 44, 95, 240, 400, 650, 950];\n  const typeData = {\n    snow:    { req: 0,  gain: .88, size: .35, score: 5,  color: new BABYLON.Color3(.94,.98,1) },\n    snowman: { req: 16, gain: 2.8,  size: .72, score: 30, color: new BABYLON.Color3(.92,.97,1) },\n    fence:   { req: 44, gain: 4.0,  size: .82, score: 55, color: new BABYLON.Color3(.48,.27,.14) },\n    tree:    { req: 95, gain: 6.2,  size: 1.05, score: 90, color: new BABYLON.Color3(.12,.42,.22) },\n    car:     { req: 240, gain: 9.0, size: 1.18, score: 150, color: new BABYLON.Color3(.85,.18,.14) }\n  };\n\n  const saveKey = 'snowball-test-v232';\n  const meta = loadMeta();\n\n  let engine, scene, camera, light, ball, shadowDisc, locatorRing, trail, grooveMaterial;\n  let powderZones = [];\n  let snowTracks = [];\n  let rampZones = [];\n  let rampMeshes = [];\n  let state = null;\n  let obstacles = [];\n  let finishTargets = [];\n  let finishDecor = [];\n  let iceZones = [];\n  let rollingLogs = [];\n  let lethalRocks = [];\n  let crevasseTraps = [];\n  let iceSpikeTraps = [];\n  let rockfallTraps = [];\n  let progressionGates = [];\n  let debris = [];\n  let pointerActive = false;\n  let pointerTargetX = 0;\n  let keys = { left:false, right:false };\n  let toastTimer = 0;\n  let audioCtx = null;\n\n  function loadMeta() {\n    try {\n      const parsed = JSON.parse(localStorage.getItem(saveKey) || '{}');\n      const clearedLevels = parsed.clearedLevels || (parsed.cleared ? {1:true} : {});\n      const hasBonusData = !!parsed.bestBonusByLevel;\n      const legacyBonus = hasBonusData ? parsed.bestBonusByLevel : {\n        1: clearedLevels[1] ? 1 : 0,\n        2: clearedLevels[2] ? 1 : 0\n      };\n      // Existing v2.5 clears receive the base \u00d71 reward once when migrating to the gold system.\n      const legacyGoldGrant = hasBonusData ? 0 : ((clearedLevels[1]?100:0) + (clearedLevels[2]?120:0));\n      return {\n        stars: Number.isFinite(parsed.stars) ? parsed.stars : 0,\n        best: Number.isFinite(parsed.best) ? parsed.best : 0,\n        startMass: Math.max(1, parsed.startMass || 1),\n        snowGain: Math.max(1, parsed.snowGain || 1),\n        impact: Math.max(1, parsed.impact || 1),\n        bestMass: Number.isFinite(parsed.bestMass) ? parsed.bestMass : 0,\n        bestStages: Number.isFinite(parsed.bestStages) ? parsed.bestStages : 0,\n        bestProgress: Number.isFinite(parsed.bestProgress) ? parsed.bestProgress : 0,\n        bestProgressByLevel: parsed.bestProgressByLevel || {1:Number.isFinite(parsed.bestProgress)?parsed.bestProgress:0,2:0},\n        runs: Number.isFinite(parsed.runs) ? parsed.runs : 0,\n        cleared: !!parsed.cleared,\n        clearedLevels,\n        unlockedLevel: Math.max(1, Number(parsed.unlockedLevel) || (parsed.cleared ? 2 : 1)),\n        gold: Math.max(0, Number(parsed.gold) || 0) + legacyGoldGrant,\n        bestBonusByLevel: legacyBonus,\n        ownedSkins: Array.isArray(parsed.ownedSkins) ? Array.from(new Set(['classic', ...parsed.ownedSkins.filter(id=>skinDefs[id])])) : ['classic'],\n        equippedSkin: skinDefs[parsed.equippedSkin] ? parsed.equippedSkin : 'classic'\n      };\n    } catch { return { stars:0, best:0, startMass:1, snowGain:1, impact:1, bestMass:0, bestStages:0, bestProgress:0, bestProgressByLevel:{1:0,2:0}, runs:0, cleared:false, clearedLevels:{}, unlockedLevel:1, gold:0, bestBonusByLevel:{1:0,2:0}, ownedSkins:['classic'], equippedSkin:'classic' }; }\n  }\n\n  function saveMeta() {\n    try { localStorage.setItem(saveKey, JSON.stringify(meta)); } catch (_) {}\n  }\n\n  function startMassValue() { return CFG.baseMass + (meta.startMass - 1) * 8.0; }\n  function snowGainMultiplier() { return 1 + (meta.snowGain - 1) * .14; }\n  function impactMultiplier() { return Math.max(.46, 1 - (meta.impact - 1) * .09); }\n  function upgradeCost(level) { return Math.min(3, 1 + Math.floor(Math.max(0, level - 1) / 3)); }\n  function runProgress() { return BABYLON.Scalar.Clamp((state?.z || 0) / CFG.finishZ, 0, 1); }\n  function currentBestProgress() { return Number(meta.bestProgressByLevel?.[activeLevel] || 0); }\n  function levelCleared(n) { return !!meta.clearedLevels?.[n]; }\n  function formatMultiplier(v) { return '\u00d7'+Number(v||1).toFixed(Number.isInteger(v||1)?0:2).replace(/0$/,''); }\n  function bestBonusForLevel(n) { return Number(meta.bestBonusByLevel?.[n] || 0); }\n  function baseGoldForLevel(n) { return Number(levelInfo[n]?.baseGold || 100); }\n  function awardFinishGold(multiplier) {\n    const base=baseGoldForLevel(activeLevel);\n    meta.bestBonusByLevel = meta.bestBonusByLevel || {1:0,2:0};\n    const previous=Math.max(0,Number(meta.bestBonusByLevel[activeLevel]||0));\n    const best=Math.max(previous,multiplier);\n    const previousClaim=Math.round(base*previous);\n    const totalClaim=Math.round(base*best);\n    const earned=Math.max(0,totalClaim-previousClaim);\n    meta.bestBonusByLevel[activeLevel]=best;\n    meta.gold=Math.max(0,Number(meta.gold)||0)+earned;\n    return {base,previous,best,multiplier,total:Math.round(base*multiplier),earned};\n  }\n  function skinColor(arr){ return new BABYLON.Color3(arr[0],arr[1],arr[2]); }\n  function applyEquippedSkin() {\n    if(!ball || !ball.material) return;\n    const skin=skinDefs[meta.equippedSkin] || skinDefs.classic;\n    ball.material.diffuseColor=skinColor(skin.diffuse);\n    ball.material.ambientColor=skinColor(skin.ambient);\n    ball.material.emissiveColor=skinColor(skin.emissive);\n    ball.material.specularColor=skinColor(skin.specular);\n    ball.outlineColor=skinColor(skin.outline);\n  }\n  function earnedForProgress(progress, bonus=0) {\n    const base = progress >= .88 ? 3 : progress >= .58 ? 2 : 1;\n    return Math.min(4, base + bonus);\n  }\n  function commitRunProgress(progress, earned, cleared=false) {\n    const previousBest = currentBestProgress();\n    const isRecord = progress > previousBest + .004;\n    meta.be";
