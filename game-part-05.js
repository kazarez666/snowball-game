window.__SNOWBALL_GAME_SRC=(window.__SNOWBALL_GAME_SRC||"")+"(0,27,14.8,22,3.7);\n  spawnObject('snowman',-3.8,31); spawnObject('snowman',3.8,38);\n  createSnowCluster(-3.5,48,5,2.2,1.35,.24); createPowderField(-4.5,58,7.2,26,4.2);\n  spawnObject('fence',3.9,64); spawnObject('tree',-4.5,77); createPowderField(4.4,86,7.0,24,4.0);\n  createSnowCluster(3.6,92,6,2.5,1.28,.26); spawnObject('tree',3.9,105); spawnObject('fence',-4.4,116);\n\n  createPowderField(0,136,15.8,30,4.6); createSnowCluster(0,142,9,4.8,1.20,.30);\n  spawnObject('tree',-4.8,155); spawnObject('tree',4.8,164); spawnObject('car',0,176);\n  createPowderField(-4.8,192,7.2,26,4.9); createPowderField(4.8,201,7.2,25,4.7);\n  createSnowCluster(-3.8,197,6,2.7,1.20,.20); createSnowCluster(3.8,206,6,2.7,1.20,.20);\n  spawnObject('car',-4.7,218); spawnObject('tree',4.5,229); spawnObject('car',1.2,240);\n\n  // First long ice sweep: safe mass line versus faster ice line.\n  createIceZone(4.2,258,7.4,38); createPowderField(-4.2,258,7.0,38,5.2);\n  createSnowCluster(-3.7,263,7,2.7,1.18,.18); spawnObject('car',4.0,276); spawnObject('tree',-4.2,286);\n  createPowderField(0,305,16.2,26,5.3); createSnowCluster(0,309,9,5.0,1.16,.26);\n  createCrevasse(332,3.7,7.0);\n  spawnObject('car',-4.8,350); spawnObject('car',4.8,361);\n\n  // Resort service road.\n  createPowderField(-4.8,392,7.0,27,5.5); createPowderField(4.8,400,7.0,27,5.5);\n  createSnowCluster(-3.8,397,6,2.7,1.16,.18); createSnowCluster(3.8,405,6,2.7,1.16,.18);\n  spawnObject('tree',-4.6,425); spawnObject('car',4.6,436); spawnObject('car',0,440);\n  createPowderField(0,462,16.0,26,5.7); createSnowCluster(0,467,9,4.8,1.14,.25);\n  createIceSpikeTrap(498,'right',9.4);\n  spawnObject('car',-4.7,520); spawnObject('tree',4.7,529);\n\n  // Optional jump: left ramp over traffic, right side stays on snow.\n  createPowderField(4.35,552,6.8,22,5.8); createSnowCluster(4.0,548,6,2.4,1.16,.18);\n  createRamp(-3.75,556,6.35); spawnObject('car',-3.75,563); createSnowCluster(-3.5,573,8,2.8,1.08,.18);\n  createPowderField(0,592,16.4,28,5.9); createSnowCluster(0,596,9,5.0,1.10,.24);\n\n  // Full-width ice with moving boulder: read timing, not mass.\n  createIceZone(0,625,17.4,36); spawnLethalRock(625,7.0,1.14,1.4);\n  createPowderField(-4.8,655,7.0,23,6.0); createPowderField(4.8,662,7.0,23,6.0);\n  spawnObject('car',-4.7,674); spawnObject('car',4.7,684); createRockfallTrap(705);\n  createSnowCluster(0,724,9,4.8,1.10,.22); createPowderField(0,728,16.0,24,6.0);\n\n  // Tight central S: alternating rich lanes.\n  createPowderField(-4.8,752,7.2,25,6.1); createSnowCluster(-3.8,755,7,2.8,1.08,.18);\n  spawnObject('tree',4.7,764); spawnObject('car',-4.7,776); spawnObject('car',3.9,806);\n  createPowderField(4.8,812,7.2,25,6.1); createSnowCluster(3.8,816,7,2.8,1.08,.18);\n  createCrevasse(856,-3.8,7.4);\n  spawnObject('car',4.7,875); spawnObject('tree',-4.6,886);\n\n  createPowderField(0,910,16.5,28,6.2); createSnowCluster(0,914,10,5.2,1.07,.24);\n  createPowderField(-4.5,940,7.0,24,6.2); createRamp(3.75,943,6.3); spawnObject('car',3.75,950); createSnowCluster(3.5,960,8,2.8,1.06,.18);\n  createIceZone(0,982,17.4,36); spawnLethalRock(982,7.1,1.06,4.2);\n  createPowderField(0,1010,16.2,22,6.3); createSnowCluster(0,1014,9,4.8,1.06,.22);\n\n  createIceSpikeTrap(1055,'left',11.0);\n  createPowderField(-4.8,1080,7.0,24,6.4); createPowderField(4.8,1087,7.0,24,6.4);\n  createSnowCluster(-3.8,1084,7,2.8,1.05,.16); createSnowCluster(3.8,1091,7,2.8,1.05,.16);\n  spawnObject('car',-4.7,1102); spawnObject('car',4.7,1112); createRockfallTrap(1132);\n  createPowderField(0,1148,16.5,18,6.5); createSnowCluster(0,1150,9,4.8,1.04,.20);\n\n  // Extra resort snow lines keep Level 2's economy comparable to Level 1 while the routing is harder.\n  const resortSnow=[\n    [0,122,14.0,18,4.8],[-4.2,232,6.8,19,5.0],[0,372,14.8,18,5.2],[-4.2,447,6.8,18,5.4],\n    [4.2,536,6.8,18,5.6],[-4.2,588,6.8,17,5.8],[0,676,14.5,18,6.0],[4.2,744,6.8,18,6.1],\n    [-4.2,824,6.8,18,6.1],[0,898,14.8,20,6.2],[-4.2,1026,6.8,19,6.3],[0,1094,14.8,18,6.4],\n    [4.2,1138,6.8,16,6.4],[0,270,14.0,16,5.0],[0,570,14.0,16,5.7],[0,960,14.0,16,6.2]\n  ];\n  resortSnow.forEach((d,i)=>{ createPowderField(...d); if(i%2===0) createSnowCluster(d[0],d[1]+2,5,2.4,1.08,.18); });\n\n  const chain=spawnObject('tree',-4.0,448); if(chain) chain.chainStarter=true;\n  spawnObject('fence',-4.0,457); spawnObject('snowman',-4.0,465); spawnObject('fence',-4.0,473);\n  const chain2=spawnObject('tree',4.0,896); if(chain2) chain2.chainStarter=true;\n  spawnObject('fence',4.0,904); spawnObject('snowman',4.0,912); spawnObject('fence',4.0,920);\n\n  createProgressionGates();\n  createFinishGauntlet();\n}\n\n\n  function createRamp(x,z,width=6.25) {\n    const worldX=trackCenterX(z)+x;\n\n    // Broad packed-snow crest. Low profile + dark underside makes it readable from far away.\n    const ramp=BABYLON.MeshBuilder.CreateBox('snowRamp',{width,height:.62,depth:7.4},scene);\n    ramp.position.set(worldX,terrainY(z)+.31,z);\n    ramp.rotation.x=SLOPE_ANGLE-.165;\n    ramp.rotation.y=Math.atan(trackTurn(z));\n    const mat=new BABYLON.StandardMaterial('rampMat'+z,scene);\n    mat.diffuseColor=new BABYLON.Color3(.94,.975,.995);\n    mat.ambientColor=new BABYLON.Color3(.74,.82,.88);\n    mat.specularColor=new BABYLON.Color3(.08,.11,.14);\n    ramp.material=mat;\n    rampMeshes.push(ramp);\n\n    const lip=BABYLON.MeshBuilder.CreateBox('rampLip',{width:width*.96,height:.13,depth:.28},scene);\n    lip.position.set(worldX,terrainY(z-3.25)+.24,z-3.25);\n    lip.rotation.x=SLOPE_ANGLE;\n    lip.rotation.y=Math.atan(trackTurn(z));\n    const lipMat=new BABYLON.StandardMaterial('rampLipMat'+z,scene);\n    lipMat.diffuseColor=new BABYLON.Color3(.35,.48,.57);\n    lipMat.specularColor=BABYLON.Color3.Black();\n    lipMat.alpha=.50;\n    lip.material=lipMat;\n    rampMeshes.push(lip);\n\n    // Two side snowbanks frame the crest without UI arrows.\n    for(const side of [-1,1]){\n      const bank=BABYLON.MeshBuilder.CreateSphere('rampBank',{diameter:2.2,segments:8},scene);\n      bank.scaling.set(1.25,.45,1.45);\n      bank.position.set(worldX+side*(width*.48),terrainY(z)+.28,z-.4);\n      bank.material=mat;\n      rampMeshes.push(bank);\n    }\n\n    rampZones.push({x:worldX,z,width,used:false});\n  }\n\n  function createSnowCluster(cx, cz, count=5, spreadX=2.4, stepZ=1.65, curve=.45) {\n    for (let i=0;i<count;i++) {\n      const t = count<=1 ? 0 : i/(count-1);\n      const x = cx + Math.sin((t-.5)*Math.PI*1.35)*spreadX*.42 + (Math.random()-.5)*.55;\n      const z = cz + (i-(count-1)/2)*stepZ + Math.sin(t*Math.PI)*curve;\n      spawnObject('snow', x, z);\n    }\n  }\n\n  function createPowderField(x,z,width,depth,rate){\n    const root=new BABYLON.TransformNode('powderField',scene);\n    const worldX=trackCenterX(z)+x;\n    root.position.set(worldX,terrainY(z),z);\n    root.rotation.x = SLOPE_ANGLE;\n    const mat=new BABYLON.StandardMaterial('powderMat'+Math.random(),scene);\n    mat.diffuseColor=new BABYLON.Color3(.90,.96,1);\n    mat.emissiveColor=new BABYLON.Color3(.02,.05,.08);\n    mat.specularColor=BABYLON.Color3.Black();\n    mat.alpha=.94;\n\n    const base=BABYLON.MeshBuilder.CreateBox('deepPowder',{width,height:.07,depth},scene);\n    base.position.y=.075; base.parent=root; base.material=mat;\n\n    // Raised lips help the powder field read as a distinct snow bank while moving.\n    for (const sx of [-1,1]) {\n      const lip=BABYLON.MeshBuilder.CreateBox('powderLip',{width:.18,height:.16,depth:depth*.96},scene);\n      lip.position.set(sx*width*.49,.11,0); lip.parent=root; lip.material=mat;\n    }\n\n    // Low, irregular mounds make it read as terrain rather than a collectible strip.\n    const count=Math.max(8,Math.round(width*depth/18));\n    for(let i=0;i<count;i++){\n      const mound=BABYLON.MeshBuilder.CreateSphere('powderMound',{diameter:1.4+Math.random()*1.4,segments:6},scene);\n      mound.scaling.set(1,.18+Math.random()*.10,1.15);\n      mound.position.set((Math.random()-.5)*(width*.86),.12,(Math.random()-.5)*(depth*.88));\n      mound.parent=root; mound.material=mat;\n    }\n    powderZones.push({root,x:worldX,z,width,depth,rate});\n  }\n\n  function createSnowRidge(zStart,zEnd,x=0){\n    const ridgeMat = new BABYLON.StandardMaterial('ridgeMat'+zStart, scene);\n    ridgeMat.diffuseColor = new BABYLON.Color3(.80,.90,.97);\n    ridgeMat.specularColor = BABYLON.Color3.Black();\n    for(let z=zStart; z<=zEnd; z+=2.15){\n      const mound=BABYLON.MeshBuilder.CreateSphere('routeRidge',{diameter:1.35,segments:6},scene);\n      mound.scaling.set(.75,.32,1.0);\n      mound.position.set(trackCenterX(z)+x+(Math.random()-.5)*.18,terrainY(z)+.15,z);\n      mound.rotation.x = SLOPE_ANGLE;\n      mound.material=ridgeMat;\n    }\n  }\n\n\n  function createIceZone(x,z,width,depth){\n    const worldX=trackCenterX(z)+x;\n    const mesh=BABYLON.MeshBuilder.CreateBox('icePatch',{width,height:.045,depth},scene);\n    mesh.position.set(worldX,terrainY(z)+.095,z);\n    mesh.rotation.x=SLOPE_ANGLE; mesh.rotation.y=Math.atan(trackTurn(z));\n    const mat=new BABYLON.StandardMaterial('iceMat'+z,scene);\n    mat.diffuseColor=new BABYLON.Color3(.40,.66,.82);\n    mat.emissiveColor=new BABYLON.Color3(.08,.14,.18);\n    mat.specularColor=new BABYLON.Color3(.85,.94,1);\n    mat.specularPower=80; mat.alpha=.76; mat.zOffset=-1; mesh.material=mat;\n    const streaks=[];\n    const streakMat=new BABYLON.StandardMaterial('iceStreak'+z,scene);\n    streakMat.diffuseColor=new BABYLON.Color3(.95,.99,1); streakMat.alpha=.62; streakMat.specularColor=BABYLON.Color3.Black(); streakMat.zOffset=-2;\n    for(let i=0;i<(width>14?8:5);i++){\n      const s=BABYLON.MeshBuilder.CreateBox('iceScratch',{width:.05+Math.random()*.05,height:.018,depth:depth*.62},scene);\n      s.position.set(worldX+(Math.random()-.5)*width*.72,terrainY(z)+.135,z+(Math.random()-.5)*depth*.12);\n      s.rotation.x=SLOPE_ANGLE; s.rotation.y=Math.atan(trackTurn(z))+(Math.random()-.5)*.05; s.material=streakMat; streaks.push(s);\n    }\n    iceZones.push({mesh,streaks,x:worldX,z,width,depth});\n  }\n\n  function currentIceZone(){\n    for(const zone of iceZones){\n      if(Math.abs(state.x-zone.x)<=zone.width*.5 && Math.abs(state.z-zone.z)<=zone.depth*.5) return zone;\n    }\n    return null;\n  }\n\n\nfunction createCrevasse(z,gapX=0,gapWidth=6.0){\n  const root=new BABYLON.TransformNode('crevasseTrap',scene);\n  const cx=trackCenterX(z);\n  root.position.set(cx,terrainY(z),z);\n  root.rotation.y=Math.atan(trackTurn(z));\n\n  const crackMat=new BABYLON.StandardMaterial('crevasseDark'+z,scene);\n  crackMat.diffuseColor=new BABYLON.Color3(.02,.03,.05);\n  crackMat.emissiveColor=new BABYLON.Color3(.01,.01,.02);\n  crackMat.specularColor=BABYLON.Color3.Black();\n  const rimDark=new BABYLON.StandardMaterial('crevasseRimDark'+z,scene);\n  rimDark.diffuseColor=new BABYLON.Color3(.08,.14,.18);\n  rimDark.specularColor=BABYLON.Color3.Black();\n  const iceMat=new BABYLON.StandardMaterial('crevasseIce'+z,scene);\n  iceMat.diffuseColor=new BABYLON.Color3(.80,.96,.99);\n  iceMat.emissiveColor=new BABYLON.Color3(.12,.24,.32);\n  iceMat.specularColor=new BABYLON.Color3(.18,.26,.32);\n  iceMat.specularPower=30;\n  const poleRed=new BABYLON.StandardMaterial('crevassePoleRed'+z,scene);\n  poleRed.diffuseColor=new BABYLON.Color3(.88,.12,.12); poleRed.specularColor=BABYLON.Color3.Black();\n  const poleWhite=new BABYLON.StandardMaterial('crevassePoleWhite'+z,scene);\n  poleWhite.diffuseColor=new BABYLON.Color3(.98,.99,1); poleWhite.specularColor=BABYLON.Color3.Black();\n\n  const half=CFG.trackHalfWidth+.55;\n  const gapL=gapX-gapWidth*.5, gapR=gapX+gapWidth*.5;\n  const segments=[[-half,gapL],[gapR,half]];\n  for(const [a,b] of segments){\n    const w=Math.max(.1,b-a);\n    const crack=BABYLON.MeshBuilder.CreateBox('crevasse',{width:w,height:.26,depth:2.55},scene);\n    crack.position.set((a+b)*.5,-.05,0); crack.parent=root; crack.material=crackMat;\n    const darkPlate=BABYLON.MeshBuilder.CreateBox('crevassePlate',{width:w+.20,height:.03,depth:2.68},scene);\n    darkPlate.position.set((a+b)*.5,.005,0); darkPlate.parent=root; darkPlate.material=rimDark;\n    for(const edge of [a,b]){\n      const rim=BABYLON.MeshBuilder.CreateBox('crevass";
